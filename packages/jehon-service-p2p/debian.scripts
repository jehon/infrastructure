#!/bin/sh

set -o errexit

# shellcheck source=SCRIPTDIR/../jehon/usr/sbin/jh-deb-helpers.sh
. /usr/sbin/jh-deb-helpers.sh

jh_deb_helper_user jehon-p2p
jh_deb_helper_ufw "app" "jehon-transmission" "home"
jh_deb_helper_ufw "app" "jehon-transmission" "homeIPv4"

case "$1" in
configure) ;;

remove) ;;

esac

#
# Legacy
#

exit 0
