#!/usr/bin/env bash

RADIX="sda"
LAST_PART="$(basename "$(find /dev/ -name "${RADIX}*" | sort | tail -n 1)")"
PART_NUM="${LAST_PART//${RADIX}/}"
echo "Extending $RADIX #${PART_NUM} ($LAST_PART)"

growpart "/dev/$RADIX" "$PART_NUM" || true

resize2fs "/dev/$LAST_PART" || true
