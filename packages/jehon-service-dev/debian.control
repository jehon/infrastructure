Depends: ${misc:Depends},
	jehon (<= JH_VERSION),
# TODO: some of these packages must go to jehon-service-desktop
	build-essential,
	cifs-utils,
# luks volumes:
	cryptsetup, cryptsetup-initramfs,
# Java (see )
	default-jdk, maven,
# Manipulate sdcard for pi
	crudini,
	direnv,
# gparted and fat32
	gparted, dosfstools, exfat-fuse, mtools,
# ffmpeg and companies
	ffmpeg, gstreamer1.0-plugins-bad, gstreamer1.0-plugins-ugly,
	gcc,
	gnupg2,
	imagemagick-6.q16,
	iputils-ping,
	jq,
	lftp,
	make,
	nmap,
	nodejs,
	openssh-client,
# Linting in vscode
	php, php-xml,
	samba,
# Version is now ok in ubuntu
	shellcheck,
# jh-music-canonize:
	sox, libsox-fmt-mp3,
	terminator,
	traceroute,
# xml_grep (jh-jdk)
	xml-twig-tools,
	xvfb,
# Dandling coma
	bash
