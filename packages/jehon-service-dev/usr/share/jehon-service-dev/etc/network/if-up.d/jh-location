#!/usr/bin/env bash

set -o errexit

# shellcheck source-path=SCRIPTDIR/../../../../../../../jehon/usr/bin
. jh-lib

exec >& >(tee -a /var/log/"$(basename "$0")")

date

export DEBUG=1

jh_debug "PRJ: ${JH_PKG_FOLDER}"
