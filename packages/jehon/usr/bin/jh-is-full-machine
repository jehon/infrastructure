#!/usr/bin/env bash

set -o errexit

# shellcheck source-path=SCRIPTDIR
. jh-lib

PID1="$(ps --no-headers -o comm 1)"

if [ "$PID1" != 'systemd' ]; then
    jh_fatal "Ko, systemd is not pid 1"
fi

echo "Ok, systemd is running"
