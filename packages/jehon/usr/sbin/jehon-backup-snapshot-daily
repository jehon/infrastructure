#!/usr/bin/env bash

# shellcheck source-path=SCRIPTDIR/../bin/
. jh-lib

_SD="$(dirname "$(realpath "${BASH_SOURCE[0]}")")"

"$_SD/jehon-backup-take-snapshot.sh" daily

# Remove too old backups: 40 days old
find "/var/backups/daily" -mtime +40 -delete
