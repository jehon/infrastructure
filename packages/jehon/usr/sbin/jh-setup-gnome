#!/usr/bin/env bash

set -o errexit
set -o pipefail
shopt -s nullglob

# shellcheck source=SCRIPTDIR/../bin/jh-lib
. jh-lib

update-alternatives --set x-terminal-emulator /usr/bin/terminator

# TODO: where to hook this?
