#!/bin/sh

set -o errexit

case "$1" in
install | upgrade)
	dpkg-divert --rename --divert /etc/minidlna.conf.diverted /etc/minidlna.conf
	;;

purge | remove)
	dpkg-divert --remove --rename --divert /etc/minidlna.conf.diverted /etc/minidlna.conf
	;;
esac

#
# Legacy
#

exit 0
