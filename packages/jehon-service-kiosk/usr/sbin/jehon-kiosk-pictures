#!/usr/bin/env bash

set -o errexit
set -o pipefail

TZ="$(cat /etc/timezone)"
TZ="${TZ:-UTC}"
export TZ

echo "Starting jh-fo in kiosk mode at $(date)"
exec /usr/bin/jh-fo kiosk --amount=30 /mnt/cloud/photos/
