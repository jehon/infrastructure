---

- name: Build dev artifacts
  hosts: all
  gather_facts: false

  tasks:
    - name: Upload init script
      ansible.builtin.template:
        src: "templates/bootstrap.sh"
        dest: "bootstrap.sh"
        mode: "0755"

    - name: Upload jehon.deb
      ansible.builtin.get_url:
        url: "https://jehon.github.io/packages/jehon.deb"
        dest: "jehon.deb"
        mode: "0644"
